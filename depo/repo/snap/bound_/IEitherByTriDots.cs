namespace nilnul.fs.git.depo.repo.snap.bound_
{
	/// <summary>
	/// "...",
	/// reachable from either one of r1 (left side) or r2 (right side) but not from both;
	/// that is remove common ancestry, and concentrate on the fork|divergence.
	/// </summary>
	interface IEitherByTriDots { }

}
