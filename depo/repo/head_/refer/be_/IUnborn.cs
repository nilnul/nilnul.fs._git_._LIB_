namespace nilnul.fs.git.depo.repo.head_.branchish.be_
{
	/// <summary>
	/// for instance when the module is just initialized, the HEAD points to a branch that doesnot exist.
	/// </summary>
	/// alias:
	///		snapless
	///		nonexist
	///		unborn
	public interface IUnborn { }
}
