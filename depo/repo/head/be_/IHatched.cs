namespace nilnul.fs.git.depo.repo.head.be_
{
	/// <summary>
	/// in detached mode,when head points to a commit;
	/// or a branch that has commit;
	/// excluding <see cref="head_.branchish_.unborn"/>
	///
	/// in other words: <see cref="_DetachedX"/> or <see cref="head_.branchish.be_.IBorn"/>; excluding <see cref="head_.branchish.be_.IUnborn"/>
	/// </summary>
	/// <remarks>
	/// <see cref="_DetachedX"/> or <see cref="refing_.IBorn"/>
	/// </remarks>
	/// <see cref="snapp"/>
	/// alias:
	///		hatchling
	///		hatchen
	///		hatch
	///		born
	///			,confusing with <see cref="branch_.IBorn"/>. the two are different. born means it's a branch. hatched can refer to detached mode;
	public interface IHatched { }

}
