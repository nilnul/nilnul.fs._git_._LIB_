using System;
using System.Diagnostics;

namespace nilnul.fs.git.depo.pier_.wet._bak_.hence._alone
{
	/// <summary>
	/// the inPlace bak has been done, which might already be an orphanated branch.
	/// 
	///
	/// now we do a commit into orphaned branch.
	/// </summary>
	static public class _OrphanX
	{
		/// <summary>
		/// 
		/// </summary>
		/// <param name="depo"></param>
		/// <param name="bakBatch"></param>
		/// <param name="git"></param>
		/// <exception cref="Exception"></exception>
		/// <exception cref="fs.git.repo.act_.xpn_.FsAccessDenied"></exception>
		static public string _Tag0nul_0depo8wet_1head_2bak(
			string depo
			,
			string oldHead
			,
			string bakBatch
			,
			nilnul.os.prog_.Git git = null
		)
		{
			if (nilnul.fs.git.depo.repo.head.be_._HatchedX._Be_0depo(depo,git))
			{
				// we need the orphan

				return _orphan_._IfHatchedX._Tag_0wet1hatchedDepo_1head_2bak(depo, oldHead,bakBatch,git);


			}
			// otherwise, the commited index is already an orphan

			return null;






		}
	}
}
