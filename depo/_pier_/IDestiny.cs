namespace nilnul.fs.git.depo._pier_
{
	/// <summary>
	/// exists or not;
	/// </summary>
	/// <remarks>
	/// take <see cref="depo.IRepo"/> as the implicit base, which cannot be changed. (<see cref="depo._rack_"/> takes repo as explicit related, which can change as both are in scope and we by default disallow sideeffect;)
	/// </remarks>
	/// vs:
	///		<see cref="IDossier"/> which must exist.
	interface IDestiny { }


}
