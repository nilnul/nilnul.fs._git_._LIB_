using System;
using System.Linq;
using nilnul.fs._address.dst;

namespace nilnul.fs.git.module.work.be_
{
	/// <summary>
	/// only :
	///		ignore files
	///		nilnulCfg settings
	///		gitattributes
	/// </summary>
	/// alias:
	///		noncrop
	///		noncreative
	///		chore
	[Obsolete()]
	static public class _TriviaX
	{
		[Obsolete()]
		static public bool Be(nilnul.fs.folder_.git_.work_.Top work) {

			return Nonwrought.Singleton.be(work);

			//var dsts = nilnul.fs.folder._Dsts.Dsts(work.en).Take(2);
			//return dsts.Count()==1 && nilnul.fs._address.dst.Eq.Singleton.Eq(
			//	dsts.First() 
			//	,
			//	".git"
			//);
		}

		//static public bool Be1(nilnul.fs.folder_.git_.work_.Top work) {

		//	return Nonwrought.Singleton.be(work);
			
		//}

		
	}
}
