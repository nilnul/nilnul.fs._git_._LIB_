#nilnul: ignore temp files; some of these might be accounted for in windows gitignore rules:
**/~(*
**/~)*

#~$*
**/~$*


#files generated for output
#**/out)*
#**/~out)*
**/*.out(![Gg]it)*
**/*.out(![Gg]it).*


# excluded; not controlled by version
**/*(!Git/
**/*(!git/

#**/*(![Gg]it)
#**/*(![Gg]it).*

*(![Gg]it)*

#**/![Gg]it)*/



#nilnul retiring a repo by renaming it to the ~{reason}{date}).git
*).git/
*(.git/
*(!git/




#nilnul: makeing a repo from inside a repo; to avoid conflicts with parent repo, name a special folder in place; this folder is named: (Git

*([Gg]it/

#*([Gg]it)*/
#[Gg]it)*/



#cfg files; unless added explicitly by a specific repo
.nilnulCfg
.nilnul

# dot led dnts are considered settings tackled by bot, thus not source code. We here allowed for dotLed doc.
.*/
**/.*/
.*

#but not .gitignore
!.gitignore

#nilnul: packages; possibly a symlink to another folder. without this ignore rule, git treats this as a file.
(packages)

# sketchup backup file. it's redundant to *.skp
*.skb


