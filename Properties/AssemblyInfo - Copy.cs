/*
  we use git.exe, not libGit, 'cuz:

		we generally already installed git.exe; So we better reuse it without installing another lib.

But if git.exe is built on lib, we may be in good process to use the lib;
 
 */